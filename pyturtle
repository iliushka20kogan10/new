from turtle import *

colormode(255)
speed(100)
colors = ['red', 'orange', 'yellow', 'green', 'cyan', 'blue', 'violet']
for i in range(72):
    color(colors[i%len(colors)])
    begin_fill()
    for i in range(6):
        forward(100)
        right(60)
    right(5)
    end_fill()

done()
